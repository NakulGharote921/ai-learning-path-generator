# TODO

- [ ] Verify dashboard request route and ensure it always waits for Firebase auth readiness before calling `getDashboard()`.
- [ ] If auth is not ready, prevent the dashboard call to avoid backend 401.
- [ ] (Optional) Add more informative error handling for missing Authorization.
- [ ] Update any dependent components/services if needed.
- [ ] Run frontend/backend checks (lint/tests) to confirm dashboard loads.

